# Versions these results were measured with (Python 3.12, CUDA 12.8, H200).
# SGLang is installed separately from the patched source -- see sglang/apply.sh.
torch==2.9.1
transformers==4.57.1
safetensors==0.7.0
accelerate==1.13.0
datasets==5.0.1
numpy==2.3.5
triton==3.5.1
huggingface-hub
